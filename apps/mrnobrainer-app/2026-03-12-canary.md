# MRnObrainer Canary - 2026-03-12

## Priority Regressions (onboarding, permissions, capture health)
- `Onboarding E2E`: **NOT EXECUTED (blocked)**. `wdio` cannot start because the Tauri release executable is missing (`Could not find mrnobrainer-app.exe in any of the expected locations`).
- `Permissions`: **NOT COVERED in this run**. No non-interactive permission-flow E2E could run without a built app binary/driver.
- `Capture health`: **No regression detected in smoke scope**. `lib/__tests__/capture-verification.test.ts` passed (2/2).

## Command Results
- `bun run doctor`: **PASS with warnings**
  - Warnings:
    - `NEXT_PUBLIC_POSTHOG_KEY` not set.
    - `NEXT_PUBLIC_SENTRY_DSN` / `SENTRY_DSN` not set.
- `bun run smoke`: **PASS**
  - 3 test files passed, 7 tests passed.
  - Included onboarding/capture-focused unit smoke tests:
    - `lib/__tests__/onboarding-flow.test.ts` (3 passed)
    - `lib/__tests__/capture-verification.test.ts` (2 passed)
- `bunx wdio run wdio.conf.js --spec ./e2e/tests/health-check.js --spec ./e2e/tests/onboarding-flow.js`: **FAIL (preflight config)**
  - Error: missing Tauri executable expected by `wdio.conf.js`.

## Skipped / Blocked Checks
- `e2e/tests/health-check.js` (blocked before test start)
- `e2e/tests/onboarding-flow.js` (blocked before test start)
- Any other WDIO health/onboarding E2E checks are currently blocked by the same missing binary prerequisite.

## Risks
- `High`: Onboarding and health-check E2E behavior is unverified for this canary run.
- `Medium`: Permission UX/regression risk remains unknown because permission-path E2E coverage could not execute.
- `Low`: Observability is reduced due to missing PostHog/Sentry keys in this environment (does not block local smoke).

## Suggested Unblock for Next Canary
1. Build the Tauri app binary in `src-tauri/target/release/` (or platform-expected release path).
2. Ensure `tauri-driver` is available on PATH (`$HOME/.cargo/bin/tauri-driver`).
3. Re-run focused E2E specs:
   - `./e2e/tests/health-check.js`
   - `./e2e/tests/onboarding-flow.js`
